// declare uma variável de nome weight

//let weight

// Que tipo de dado é a variável acima

//console.log(typeof weight)

// Declare uma variável e atribua valores para cada um dos dados

/*
// name = string
const name = "Pietro";
// age = number
const age = 26;
// stars = float
const stars = 10.987;
// Subscribed: boolean
const Subscribed = true;
*/

// A variável student abaixo é de que tipo de dado:

//Atribua a ela as mesmsas propriedades e valores no exercicio 3

//mostre no console a seguinte mensagem



let student = {
  name: 'Ichigo',
  age: 18,
  weight: 75.5,

};

/*
console.log(typeof student)
console.log(`${student.name} de idade ${student.age} pesa ${student.weight} kg`)
*/



//Declare uma variável do tipo array, de nome students e não atribua nenhum valor 

//const students = []

//Retribua o valor para a variável acima, colocando dentro dela o objeto student da questão 4.
let students = [student]

// Coloque no console o valor da posição zero do Array acima
//console.log(students[0])


//Crie um novo student e coloque students na posição 1 do array

const Itadori = {
  name:'Itadori',
  students,
  age: 25,
  weight: 80.5,
  isSubscribed: true,
}

students = [
  student,
  Itadori
]
console.log(students[1])

// Sem rodar o código responda qual é a resposta aaixo e por que? após sua resposta, rode o código e veja se você acertou

console.log(a)
var a = 1;
console.log(a)